version: "2.2"
dependencies:
  - project: andzuc/debian-base
  - project: andzuc/debian-scilan
    dependencies:
      - project: andzuc/debian-base
default:
  build-command:
    upstream: echo "upstream"
    current: echo "current"
    downstream: echo "downstream"
